Edit an existing file by replacing one exact occurrence of old_string with new_string (path relative to the workspace root). Use this to MODIFY existing code — a targeted edit costs a few hundred tokens instead of re-emitting the whole file, keeps the context small, and never risks truncating the file. old_string must appear exactly once in the file; if it is missing or ambiguous the tool errors and asks for more context — include surrounding lines to make it unique. The result shows a unified diff. For NEW files use write_file.
