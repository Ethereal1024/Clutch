# Refactoring Recommendations

> Generated by the refactor skill. Review each item and tell Claude which ones to implement.
> 用户可用编号（R1…R8）选择性批准，例如「implement all of them」或「implement R1, R3, R6」。

## Summary

clutch 的核心架构总体是清晰、自洽的：事件流作为唯一事实源、错误即数据、
工具注册表单一职责、本地/远程通过 Workspace 抽象对齐，都是成立的设计。
主要的复杂度问题集中在**少量重复知识**（同一概念在 3~4 个地方各实现一遍）和
**两个超大模块**（`server.py` 的 Handler 与 `loop.py` 的 compaction 逻辑）。
没有发现需要大动干戈的结构性问题；以下建议都是行为保持、低风险的增量化简。

测试网络已存在且相当扎实（`selfcheck.py` 覆盖解析/上下文/工作区安全/工具/权限/
项目/记忆/技能/代理，`loop_test.py`、`server_test.py`、`transport_test.py` 覆盖
循环与 HTTP+SSE），本次建议的每一项都能被现有测试直接验证。

---

## ⚠ Safe Refactoring Mode（可选）

本仓库规模中等、已有可用的测试网络，但 `loop.py` 与 `server.py` 属于关键路径、
改动会跨模块（R7）。若希望额外加一道保险，可对 R7（及 R4/R5 的服务端部分）
启用 Safe Refactoring Mode：先写 characterization tests 钉住现状，再在测试网络内
重构。其余建议（R1–R3、R6、R8）风险很低，现有测试已足够。

- 永久启用：告诉 Claude **"enable safe refactoring mode"**（将创建 `.refactor-safe-mode`）
- 本次跳过：**"implement without safe mode"**

---

## Recommendations

### R1 · DRY / SRP · `agent/base.py`（RunState.set_project ↔ BaseServer.start_task）

**Issue:** 同一次运行会构建**两个** Workspace 实例：`set_project` 先构建一个并
`protect(project.path)`（base.py:85-93），`start_task` 又调用 `build_workspace` 重新
构建一个并再次 `protect`（base.py:152）。`protect` 调用点分散在两处，职责被拆分；
UI 文件树用的 workspace 与 agent 实际执行用的 workspace 不是同一个对象。

**Recommendation:** `start_task` 里复用 `state.workspace`：当它非空且
`workspace.root == project.workdir` 时直接使用，否则才新建；`protect` 收敛到
`start_task` 一处（eval harness 直接调 `start_task` 不经 `set_project`，所以
`start_task` 必须保留兜底新建逻辑——这正是复用条件要覆盖的）。

**Benefit:** 一次运行一个 workspace 实例；保护逻辑单一入口；行为完全不变。

### R2 · DRY · `agent/base.py`（build_llm ↔ compactor 工厂闭包）

**Issue:** `build_llm()`（base.py:117-126）与 `_make_compactor` 闭包（base.py:158-171）
逐参数重复同一份 `create_llm_client(provider="openai", api_key=…, model=…, base_url=…,
request_timeout=…, max_retries=…, retryable_status=…)` 调用，仅 api_key/model 不同。
这是全仓库仅有的两处 LLM 客户端装配，签名一旦漂移（如新增超时参数）就要改两处。

**Recommendation:** 提取私有助手 `BaseServer._build_llm(self, api_key: str, model: str,
cfg: Config) -> LlmClient`，`build_llm` 与闭包都调用它。

**Benefit:** LLM 客户端装配只有一处定义；compactor 逻辑更短更直白。

### R3 · DRY · `agent/events.py` + `agent/memory.py` + `agent/project.py`（.clc 行追加持久化）

**Issue:** “向 .clc 追加一行 JSONL，支持 writer 回调（远程桥）/本地 open 兜底”这一
知识重复了三遍：`EventLog.append`（events.py:176-184）、`MemoryStore._append`
（memory.py:76-82）、以及 `project.py` 里 `writer = workspace.append_line if …`
的取值行（create_project / open_project 各一次）。本地与远程的持久化行为靠三处
手工保持一致。

**Recommendation:** 在 `core/persist.py`（或直接放 `events.py`）提供一个小函数
`append_jsonl(path, line, writer=None)`：有 writer 走 writer，否则本地追加。
`EventLog.append` 与 `MemoryStore._append` 都改为调用它；`project.py` 的 writer 取值
收敛为一行。

**Benefit:** “写 .clc”只有一份实现；今后新增持久化消费者（如 settings 落盘）
直接复用。selfcheck 的项目/记忆回环测试即可验证。

### R4 · DRY · `agent/server.py` + `agent/tools/workspace.py`（远程 ls 输出解析 + 目录节点塑形）

**Issue:** `ls -1AF` 输出的解析（`/` 结尾=目录、去掉 `*/@` 后缀、隐藏文件过滤）在
三处各实现一遍：`RemoteWorkspace._parse_list_output`（workspace.py:287-298）、
`Handler._fs_list_remote`（server.py:394-408）、`_walk_remote`（server.py:543-553）。
同时目录树节点的塑形（`{name, path, dir, link}` + “是否展开该层”判定
`rel == "" or child_rel in expanded or rel in expanded`）在 `_walk` 与 `_walk_remote`
两处重复（server.py:602 与 552）。四个月录/树站点共享同一份“远程目录是什么样”的
知识，却各自实现，wire 格式一改就要四处同步。

**Recommendation:** 在 `workspace.py`（或 `transport.py`）放一个
`parse_ls_entries(stdout) -> list[(name, is_dir)]` 助手，三个远程解析点统一调用；
再把“节点塑形 + 是否列子目录”的判定提取为 `_tree_node` / `_should_list` 助手，
`_walk` 与 `_walk_remote` 共用（**只共享塑形与判定，不合并遍历策略**——本地递归与
远程按层批量 `list_many` 是真实差异，必须保留）。

**Benefit:** 远程目录 wire 格式只有一份解析；树节点语义单一来源；四处站点不再漂移。

### R5 · SRP · `agent/server.py`（`_backend` 直接改写 4 个 state 字段）

**Issue:** `Handler._backend`（server.py:330-372）的 ssh 与 local 两个分支都手工重置
`project/workspace/gate`（再各设 backend_mode/bridge_url/remote_root），Handler 直接
钻进 `RunState` 的内部字段做状态迁移，迁移逻辑散落在 HTTP 层。

**Recommendation:** 给 `RunState` 增加 `set_backend(mode, bridge_url=None, remote_root=None)`，
把模式切换 + 失效清理封装成状态自己的方法；`_backend` 只负责解析 body 后调用它。
两个分支收敛为一个方法。

**Benefit:** 状态迁移与状态同处一地；Handler 不再知晓 RunState 内部结构；
新增后端模式时只改一处。

### R6 · SRP（可选、中等风险）· `agent/loop.py`（compaction 机制移出主循环）

**Issue:** `loop.py` 约 400 行，主循环（run/_execute_tool/_llm_call）与整块
compaction 机制（`_should_compact/_estimate_tokens/_compact/_tail_start_index/
_previous_summary/_serialize/_summarize`，约 loop.py:223-360）混在一个类里。
读者看“循环在做什么”时被迫同时消化“token 估算、tail 预算、序列化、摘要调用”
这些上下文管理细节——而上下文管理本属 `core/context.py` 的职责。

**Recommendation:** 把 compaction 块抽为 `core/compaction.py` 里的一个
`Compactor`（持有 config/log/llm 工厂，提供 `should_compact(msgs)`、`compact()`），
`Agent` 持有一个 `Compactor` 实例。`run()` 只保留“该不该压、压没压成”两个调用点。

**Benefit:** `loop.py` 回归“循环是什么”；compaction 可独立测试；`core/context.py`
与 compaction 的边界更清晰。**风险点：** 这是最热的路径，建议配合 safe mode 的
characterization tests（`loop_test.py` 已覆盖大部分路径）逐步迁移。

### R7 · KISS · 小清理（顺手项，可与任意项合并）

- `server.py` `_walk` 的 docstring 中 `show_hidden keeps dotfiles out of every level.`
  因缩进断裂落在独立一行（server.py:571），合并进 docstring 正文。
- `project.py` 中 `writer = workspace.append_line if workspace is not None else None`
  在 create_project 与 open_project 各出现一次——随 R3 一并收敛。
- `server.py` `_replace()` 只是 `dataclasses.replace` 的别名，仅一处使用；可保留
  （有注释价值）也可删掉直接内联，二选一即可。

---

## 明确不动的部分（有意的复杂度）

- **LocalWorkspace / RemoteWorkspace 双实现**：本地走 Path 操作、远程走 exec 桥的
  差异是真实本质差异（往返批处理、分块写入、sshd 大小限制），Workspace 抽象已把
  工具层与传输层解耦得很好，不合并。
- **`_walk` 递归 vs `_walk_remote` BFS 遍历策略**：远程按层单次往返是性能要求，
  两套遍历策略保留（R4 只共享塑形与判定）。
- **skills 的极简 frontmatter 解析器**：仅支持 `key: value` 是有意约束，不引入 YAML 依赖。
- **ChunkHandler 流水线**：reasoning/text/tool_call/finish 四个 handler 各司其职，
  结构已足够扁平。
- **`RunState` 的 busy/cancel/gate 字段**：与 server 的交互方式虽然直接，但在当前
  规模下是简单方案，不值得引入状态机框架。

---

## 建议实施顺序

1. **R1 + R2**（base.py，同文件相邻改动，一次 diff）
2. **R3**（persist 助手 + 三处接入，selfcheck 直接验证）
3. **R4**（ls 解析助手 + 树塑形共用，server_test/transport_test 验证）
4. **R5**（RunState.set_backend，server_test 验证）
5. **R7**（顺手清理）
6. **R6**（可选，单独做，建议先上 characterization tests）

请审阅后告诉我实施哪些（例如「implement all of them」或「implement R1, R3, R5」）。
未获批准前不会改动任何源文件。
